before_install:
  - apt-get -qq update
  - apt-get install -y libgsl0ldbl
  - apt-get install gsl-bin
language: r
r:
  - devel
env:
  global:
    - R_CHECK_ARGS="--as-cran"
sudo: false
cache: packages
warnings_are_errors: true

